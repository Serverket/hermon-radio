# Backend env example
PORT=8080
# Optional Postgres (leave empty for in-memory)
DATABASE_URL=
# Admin credentials for Basic Auth
ADMIN_USER=admin
ADMIN_PASS=changeme
# Comma-separated CORS origins (no spaces)
CORS_ORIGINS=http://localhost:5173,https://your-frontend.vercel.app
